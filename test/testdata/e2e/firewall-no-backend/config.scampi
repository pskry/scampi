module main

import "std"
import "std/posix"
import "std/local"

let host = local.target { name = "local" }

std.deploy(name = "test", targets = [host]) {
  posix.firewall {
    desc = "allow SSH"
    port = "22/tcp"
    action = posix.FirewallAction.allow
  }
}
