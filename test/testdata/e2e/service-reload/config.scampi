module main

import "std"
import "std/posix"

let local = posix.local { name = "local" }

std.deploy(name = "test", targets = [local]) {
  posix.service {
    desc = "reload nginx"
    name = "nginx"
    state = posix.ServiceState.reloaded
  }
}
