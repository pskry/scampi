module main

import "std"
import "std/posix"
import "std/local"

let host = local.target { name = "local" }

std.deploy(name = "test", targets = [host]) {
  posix.service {
    desc  = "reload nginx"
    name  = "nginx"
    state = posix.ServiceState.reloaded
  }
}
