module main

import "std"
import "std/posix"

let local = posix.local { name = "local" }

std.deploy(name = "test", targets = [local]) {
  posix.group {
    desc = "create group appusers"
    name = "appusers"
    gid = 1100
  }
}
