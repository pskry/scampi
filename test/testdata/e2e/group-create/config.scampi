module main

import "std"
import "std/posix"
import "std/local"

let host = local.target { name = "local" }

std.deploy(name = "test", targets = [host]) {
  posix.group {
    desc = "create group appusers"
    name = "appusers"
    gid = 1100
  }
}
