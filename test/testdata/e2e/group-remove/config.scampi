module main

import "std"
import "std/posix"
import "std/local"

let host = local.target { name = "local" }

std.deploy(name = "test", targets = [host]) {
  posix.group {
    desc  = "remove group appusers"
    name  = "appusers"
    state = posix.GroupState.absent
  }
}
