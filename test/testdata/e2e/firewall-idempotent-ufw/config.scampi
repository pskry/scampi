module main

import "std"
import "std/posix"

let local = posix.local { name = "local" }

std.deploy(name = "test", targets = [local]) {
  posix.firewall {
    desc = "SSH already allowed"
    port = "22/tcp"
    action = posix.FirewallAction.allow
  }
}
