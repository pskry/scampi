module main

import "std"
import "std/pve"
import "std/local"

let host = local.target { name = "local" }

std.deploy(name = "test", targets = [host]) {
  pve.datacenter {
    mac_prefix = "not-a-mac"
  }
}
