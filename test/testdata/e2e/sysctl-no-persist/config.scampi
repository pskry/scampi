module main

import "std"
import "std/posix"
import "std/local"

let host = local.target { name = "local" }

std.deploy(name = "test", targets = [host]) {
  posix.sysctl {
    desc    = "set live only"
    key     = "net.ipv4.tcp_keepalive_time"
    value   = "300"
    persist = false
  }
}
