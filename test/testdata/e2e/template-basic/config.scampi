module main

import "std"
import "std/posix"
import "std/local"

let host = local.target { name = "local" }

std.deploy(name = "test", targets = [host]) {
  posix.template {
    desc = "render greeting"
    src = posix.source_local { path = "/tmpl/greeting.txt" }
    dest = "/tmp/greeting.txt"
    data = {
      "values": {
        "name": "world",
        "count": 3,
      },
    }
    perm = "0644"
    owner = "testuser"
    group = "testgroup"
  }
}
