module main

import "std"
import "std/posix"
import "std/local"

let host = local.target { name = "local" }

std.deploy(name = "test", targets = [host]) {
  posix.firewall {
    desc  = "allow DNS"
    port  = 53
    proto = posix.FirewallProto.udp
  }
}
