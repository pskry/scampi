module main

import "std"
import "std/posix"

let local = posix.local { name = "local" }

std.deploy(name = "test", targets = [local]) {
  posix.run {
    desc = "always-run that fails"
    apply = "fail-cmd"
    always = true
  }
}
