module main

import "std"
import "std/posix"

let local = posix.local { name = "local" }

std.deploy(name = "test", targets = [local]) {
  posix.run {
    desc = "apply that fails"
    check = "check-thing"
    apply = "apply-thing"
  }
}
