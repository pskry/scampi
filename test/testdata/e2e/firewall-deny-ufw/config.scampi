module main

import "std"
import "std/posix"

let local = posix.local { name = "local" }

std.deploy(name = "test", targets = [local]) {
  posix.firewall {
    desc = "deny HTTPS"
    port = "443/tcp"
    action = posix.FirewallAction.deny
  }
}
