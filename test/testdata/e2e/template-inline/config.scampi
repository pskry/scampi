module main

import "std"
import "std/posix"

let local = posix.local { name = "local" }

std.deploy(name = "test", targets = [local]) {
  posix.template {
    desc = "render inline"
    src = posix.source_inline { content = "server={{ .host }}:{{ .port }}" }
    dest = "/tmp/config.txt"
    data = {
      "values": {
        "host": "localhost",
        "port": 8080,
      },
    }
    perm = "0600"
    owner = "testuser"
    group = "testgroup"
  }
}
