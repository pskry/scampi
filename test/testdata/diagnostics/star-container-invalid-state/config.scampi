module main

import "std"
import "std/posix"
import "std/container"

let host = posix.local { name = "host" }

std.deploy(name = "main", targets = [host]) {
  container.instance { name = "test", image = "nginx:latest", state = "bogus" }
}
