module main

import "std"
import "std/posix"

let host = posix.local { name = "host" }

std.deploy(name = "main", targets = [host]) {
  posix.pkg { packages = ["vim"], state = "purge", source = posix.pkg_system {} }
}
