module main

import "std"
import "std/posix"

let host = posix.local { name = "host" }

std.deploy(name = "main", targets = [host]) {
  posix.dir { path = "/tmp/a" }
}
std.deploy(name = "main", targets = [host]) {
  posix.dir { path = "/tmp/b" }
}
