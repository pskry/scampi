# Ansible runtime for the bench. Pinned for reproducibility — bump
# only when re-baselining the numbers. Both bench runs (scampi side
# and ansible side) must use the versions captured here.
#
# Install into a venv (see ../README.md for the full setup):
#
#   python3 -m venv venv
#   ./venv/bin/pip install -r requirements.txt
#
# ansible-core ships ansible.builtin (apt, template, service) which
# is everything site.yml uses — no extra collections needed.

ansible-core==2.18.5
