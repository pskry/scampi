name: CI

on:
  # TEMP: disabled while debugging release workflow
  workflow_dispatch:

jobs:
  ci:
    runs-on: scampi
    steps:
      - uses: actions/checkout@v4
      - run: just build
      - run: just test race
      - run: just lint
